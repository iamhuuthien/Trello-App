# Server
PORT=4000

# Auth
JWT_SECRET=change_me_securely

# Firebase Admin (ví dụ split fields)
FIREBASE_PROJECT_ID=
FIREBASE_CLIENT_EMAIL=
# escape newlines with \n in PRIVATE_KEY for single-line
FIREBASE_PRIVATE_KEY=""

# SMTP (nodemailer)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=